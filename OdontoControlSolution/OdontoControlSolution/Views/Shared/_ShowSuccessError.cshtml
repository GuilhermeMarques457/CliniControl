@if (ViewBag.Success != null)
{
    <div class="status-message-box success-box">
        <div class="status-message mg-tp-md">
            <i class="ph ph-x closable-item"></i>
            <p class="text-green success-message">@ViewBag.Success</p>
        </div>
    </div>

}
@if (ViewBag.Errors != null)
{
    <div class="status-message-box error-box">
        <div class="status-message mg-tp-md">
            <i class="ph ph-x closable-item"></i>
            <p class="text-red error-message">@ViewBag.Errors</p>
        </div>
    </div>
}